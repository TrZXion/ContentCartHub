import streamlit as st
from st_pages import add_page_title, hide_pages

add_page_title()

hide_pages(["Thank you"])

st.subheader(
    """We are currently building SDE Education Content with a nominal fee! 🙌🏻"""
)
